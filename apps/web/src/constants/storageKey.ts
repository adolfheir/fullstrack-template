//加个版本号
const version = 'v1.0.0';
const prefix = `IHOUQI_${version}_`;

//是否是测试模式
export const WEB_DEV_MODE = '__FE_DEV__';
export const LOG_LEVEL = 'LOG_LEVEL';
export const TOKEN = 'TOKEN';

export const GLOBAL_USER = prefix + 'GLOBAL_USER';
export const GLOBAL_PWD = prefix + 'GLOBAL_PWD';

export const GLOBAL_TOKEN = 'authorization';

export const PAGE_DASHBOARD_TAB = prefix + 'PAGE_DASHBOARD_TAB';
